<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<!--    This file generated by program.   -->
<!--         Do Not Modify It.            -->
<!-- Genertion Time : 2022-02-26 15:56:09 -->
<mapper namespace="org.chun.plutus.common.dao.base.ActivityDtExcludeBaseDao">

    <resultMap id="vo" type="activityDtExcludeVo">
        <id column="ace_num" property="aceNum" />
        <result column="acd_num" property="acdNum" />
        <result column="user_num" property="userNum" />
        <result column="reason" property="reason" />
    </resultMap>

    <sql id="columnsAlias">
        ${alias}.ace_num                      as ${prefix}ace_num,
        ${alias}.acd_num                      as ${prefix}acd_num,
        ${alias}.user_num                     as ${prefix}user_num,
        ${alias}.reason                       as ${prefix}reason
    </sql>

    <select id="query" resultMap="vo">
        select
        <include refid="columnsAlias">
            <property name="alias" value="activityDtExclude"/>
            <property name="prefix" value=""/>
        </include>
        from activity_dt_exclude activityDtExclude
        <trim prefix="where" prefixOverrides="and | or">
            <if test="aceNum!=null">and ace_num=#{aceNum}</if>
            <if test="acdNum!=null">and acd_num=#{acdNum}</if>
            <if test="userNum!=null">and user_num=#{userNum}</if>
            <if test="reason!=null">and reason=#{reason}</if>
        </trim>
    </select>

    <select id="listAll" resultMap="vo">
        select
        <include refid="columnsAlias">
            <property name="alias" value="activityDtExclude"/>
            <property name="prefix" value=""/>
        </include>
        from activity_dt_exclude activityDtExclude
    </select>

    <select id="count" resultType="Integer">
        select count(*)
        from activity_dt_exclude
        <trim prefix="where" prefixOverrides="and | or">
            <if test="aceNum!=null">and ace_num=#{aceNum}</if>
            <if test="acdNum!=null">and acd_num=#{acdNum}</if>
            <if test="userNum!=null">and user_num=#{userNum}</if>
            <if test="reason!=null">and reason=#{reason}</if>
        </trim>
    </select>

    <insert id="insert" useGeneratedKeys="true" keyColumn="ace_num" keyProperty="aceNum">
        insert into activity_dt_exclude (
        <trim prefix="" prefixOverrides=",">
                <if test="aceNum!=null">, ace_num</if>
                <if test="acdNum!=null">, acd_num</if>
                <if test="userNum!=null">, user_num</if>
                <if test="reason!=null">, reason</if>
        </trim>
        ) values (
        <trim prefix="" prefixOverrides=",">
                <if test="aceNum!=null">, #{aceNum}</if>
                <if test="acdNum!=null">, #{acdNum}</if>
                <if test="userNum!=null">, #{userNum}</if>
                <if test="reason!=null">, #{reason}</if>
        </trim>
        )
    </insert>

    <delete id="delete">
        delete from activity_dt_exclude
        <trim prefix="where" prefixOverrides="and">
            <if test="aceNum!=null">and ace_num=#{aceNum}</if>
            <if test="acdNum!=null">and acd_num=#{acdNum}</if>
            <if test="userNum!=null">and user_num=#{userNum}</if>
            <if test="reason!=null">and reason=#{reason}</if>
        </trim>
    </delete>


    <select id="getByPk" resultMap="vo">
        select
        <include refid="columnsAlias">
            <property name="alias" value="activityDtExclude"/>
            <property name="prefix" value=""/>
        </include>
        from activity_dt_exclude activityDtExclude
        where
            ace_num=#{aceNum}
    </select>

    <delete id="deleteByPk">
        delete from activity_dt_exclude
        where
            ace_num=#{aceNum}
    </delete>

    <update id="update">
        update activity_dt_exclude
        <trim prefix="set" prefixOverrides=",">

            <if test="acdNum!=null">, acd_num=#{acdNum}</if>
            <if test="userNum!=null">, user_num=#{userNum}</if>
            <if test="reason!=null">
                <if test="reason!=''">, reason=#{reason}</if>
                <if test="reason==''">, reason=null</if>
            </if>
        </trim>
        where
            ace_num=#{aceNum}
    </update>

    <update id="forceUpdate">
        update activity_dt_exclude
        <trim prefix="set" prefixOverrides=",">
                            , acd_num=#{acdNum}

                            , user_num=#{userNum}

        <if test="reason!=''">, reason=#{reason}</if>
        <if test="reason==''">, reason=null</if>

        </trim>
        where
            ace_num=#{aceNum}
    </update>

</mapper>
